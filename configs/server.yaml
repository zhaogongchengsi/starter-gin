Server:
  Port: 3000 # 服务端口
  Host: 0.0.0.0
  #  debug release test
  Mode: debug # 运行模式
  Prefix: api/v1 # 统一url前缀
  Static: static # 静态文件目录
Jwt:
  SigningKey: starter_gin_231das_asdasd # 盐值
  Issuer: satrter_gin # 签发人
  ExpiresAt: 10 # 过期时间(分钟)
